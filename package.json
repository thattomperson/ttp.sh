{
  "scripts": {
    "icon-font": "mkdir -p static/fonts && icon-font-generator -p fa -n fa --cssfontsurl /fonts --csspath ./assets/scss/components/_fonts.scss ./assets/fontawesome/{brands/{github,twitter},regular/envelope,solid/globe-americas}.svg -o static/fonts",
    "prebuild": "npm run icon-font",
    "build": "hugo --minify",
    "now-build": "npm run build -- -d dist",
    "prestart": "npm run prebuild",
    "start": "hugo serve"
  },
  "dependencies": {
    "hugo-cli": "^0.11.0",
    "icon-font-generator": "^2.1.10"
  },
  "devDependencies": {
    "rollup": "^1.7.4"
  }
}
