{
    "scripts": {
        
        "prebuild": "mkdir -p static/fonts && icon-font-generator -n fa --cssfontsurl /fonts --csspath ./assets/scss/components/_fonts.scss ./assets/fontawesome/brands/github.svg -o static/fonts",
        "build": "hugo --minify"
    },
    "dependencies": {
        "icon-font-generator": "^2.1.10"
    }
}
