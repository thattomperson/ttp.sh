{
    "scripts": {
        
        "prebuild": "mkdir -p static/fonts && icon-font-generator -p fa -n fa --cssfontsurl /fonts --csspath ./assets/scss/components/_fonts.scss ./assets/fontawesome/{brands/{github,twitter},regular/envelope,solid/globe-americas}.svg -o static/fonts",
        "build": "hugo --minify",
        "prestart": "npm run prebuild",
        "start": "hugo serve"
    },
    "dependencies": {
        "icon-font-generator": "^2.1.10"
    }
}
